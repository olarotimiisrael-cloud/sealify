name = "sealify"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"

[env.production]
NODE_VERSION = "20"

# Remove placeholder SESSION_KV binding - not used in application
# If KV is needed later, create namespace via: wrangler kv:namespace create "SESSION_KV"
# Then add binding with actual namespace ID
